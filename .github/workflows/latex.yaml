name: Build LaTeX document
on: 
 push:
jobs:
 buildlatex:
  runs-on: ubuntu-latest
  container: texhackse/texlive-node-github
  steps:
   - run: echo "running texlive-node-github:latest"
   - name: Set up Git repository
     uses: actions/checkout@v3
   - name: to main dirt
     run: |
       cd papers/
       latexmk -r tex/CI.rc

